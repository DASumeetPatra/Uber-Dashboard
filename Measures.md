       total_bookings = COUNT(master_table[booking_id]), 
       completed_rides = COUNT(completed_rides[booking_id]), 
       cancelled_rides = CALCULATE(COUNT(master_table[booking_id]),master_table[booking_status] <> "Completed"), 
       cancellation % = ROUNDUP(DIVIDE([cancelled_rides],[total_bookings],0)*100,2)&"%", 
       peak_rides = CALCULATE([total_bookings],master_table[peak_non_peak] = "Peak"), 
       non_peak_rides = CALCULATE([total_bookings],master_table[peak_non_peak]="Non Peak"), 
       average_fare = AVERAGE(completed_rides[booking_value]), 
       total_revenue = SUM(completed_rides[booking_value]), 
       average_driver_rating = AVERAGE(completed_rides[driver_ratings]), 
       average_customer_rating = AVERAGE(completed_rides[customer_rating]), 
       avg_req_ride_distance = AVERAGE(master_table[ride_distance]), 
       avg_completd_ride_distance = AVERAGE(completed_rides[ride_distance]), 
       weekday_rides = CALCULATE([total_bookings],date_table[Is_Weekend]="Weekday"),
       weekend_rides = CALCULATE([total_bookings],date_table[Is_Weekend]="Weekend")
